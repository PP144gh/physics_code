#include <stdio.h>
#include <stdlib.h>
#include <math.h>

void  
init()
{
 
  printf("Introduzir o caso corres. à função\n");     
}



int 
main ()
{
  double x0, t0, tf, l, d,i,p,x;
  FILE *f;
  d=1e-10;
  printf("Introduza os valores de x0, t0, tf e l\n");
  scanf("%lf %lf %lf %lf, x0", &x0, &t0, &tf, &l);
  fopen(
  x=x0;
  for(i=t0, t=t0, i*d+t0<=tf, i++)
    {
      p=i*d+t0;
      printf("%lf\n",p);
      printf("%lf\n", x);
      x=x-l*d;
return 0

  
